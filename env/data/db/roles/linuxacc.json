{
    "db_name": "mysqldb",
    "creation_statements": [
      "CREATE USER '{{name}}'@'%' IDENTIFIED BY '{{password}}'",
      "GRANT SELECT ON *.* TO '{{name}}'@'%';"
    ],
    "default_ttl": "5m",
    "max_ttl": "30m"
}
